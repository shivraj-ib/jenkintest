pipeline {
    agent any

	options {
        disableConcurrentBuilds()
    }

    stages {
		stage('Get webhook params'){
			steps{
				echo "Hello i am Jenkinsfile test";
				echo "${GIT_COMMIT}";
			}
		}
    }
}